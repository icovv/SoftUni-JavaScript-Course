function reversedThings(things){
    // let reversed = ``
    // for (let i = things.length-1; i >= 0 ; i --){
    //     reversed += `${things[i]} `
    // }
    // console.log(reversed)
    console.log(things.reverse().join(` `))
}
reversedThings(['a', 'b', 'c', 'd', 'e']);
reversedThings(['33', '123', '0', 'dd']);
