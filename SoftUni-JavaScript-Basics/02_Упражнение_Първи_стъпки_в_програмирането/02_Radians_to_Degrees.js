function radsToDegs(input){

    let rads = Number(input[0]);
    let degs = ((`${rads}` * 180) / Math.PI)

    console.log(degs)


   // function radsToDegs(input){

     //   let rads = Number(input[0]);
    
       // console.log(((`${rads}` * 180) / Math.PI))



}

radsToDegs(["3.1416"])